
import { useNavigate } from 'react-router-dom';
import { useEffect } from 'react';
import Dashboard from './Dashboard';

const Index = () => {
  // The Index page now renders the Dashboard component
  return <Dashboard />;
};

export default Index;
